# Codex Status Light 支持

## 使用方法

1. 首次启动时选择个人目录中的 `.codex` 文件夹并授予只读权限。
2. 选择“刘海旁”“桌面任意位置”或“仅菜单栏”。
3. 红灯表示连接中断，黄灯表示正在运行，绿灯表示任务结束。

## 常见问题

- 没有状态：从菜单栏打开应用并重新授权 `.codex` 文件夹。
- 找不到 `.codex`：在文件选择窗口按 `Command + Shift + .` 显示隐藏文件。
- 悬浮灯位置不合适：选择桌面模式后直接拖动。

支持邮箱：huoyuan461@qq.com
